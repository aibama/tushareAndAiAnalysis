你作为python后端开发和金融领域的专家，请审阅检查PatternAnalysis\plombery和PatternAnalysis\strategy下的代码;
PatternAnalysis\strategy\ATR模块的功能基于该模块下的readme构建，现在需要完善该需求：
1、后端根据个股的基于ATR（平均真实波幅）的自适应阈值，识别个股中低波动稳定期，保存入redis/stream里面（新建主题）;
2、后端暴露一个接口（swagger可以访问），通过个股代码，统计个股的中低波动稳定期的开始时间、结束时间序列（例如一个股有两个中低波动稳定期的开始时间）。

ATR稳定区间
使用方式
启动服务后访问 http://localhost:8081/docs 查看Swagger文档
调用 /api/atr/stable-periods/{ts_code} 接口查询个股稳定期
或调用 /api/atr/stable-periods/recalculate 触发全量计算


1、调用 /api/atr/stable-periods/{ts_code} 接口查询个股稳定期的接口，需增加字段，max_stable_days就是stable_periods里面最大的duration_days;另外确认下min_stable_days的逻辑是怎样的？
2、orm目录下的申万分类功能，需后端新增一个接口，前端可以根据这个接口生成对应的目录树（目前是三级目录）