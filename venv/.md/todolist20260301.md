
你作为python后端开发专家，请按需求hold on deprecated，开发以下内容：
1、用http，get请求，访问https://q.10jqka.com.cn/gn/
得到的html，按下面思路，解析出对应结构的数据;
import requests
from bs4 import BeautifulSoup

# 获取页面内容（假设您已获取 HTML）
# response = requests.get('https://q.10jqka.com.cn/gn/')
# soup = BeautifulSoup(response.text, 'html.parser')

# 使用您提供的本地 HTML 示例
with open('page.html', encoding='utf-8') as f:
    soup = BeautifulSoup(f, 'html.parser')

# 提取所有概念
concepts = {}

# 分类区
cate_links = soup.select('.cate_items a')
for a in cate_links:
    name = a.text.strip()
    concepts[name] = a['href']

# 表格区
table_links = soup.select('.m-table tbody a[href*="/gn/detail/code/"]')
for a in table_links:
    name = a.text.strip()
    concepts[name] = a['href']
并保存到数据库中，对应的数据库表结构由你设计和提供的数据库脚本决定；
2、按上面解析到的数据，href属性（形如https://q.10jqka.com.cn/gn/detail/code/309264/）；
