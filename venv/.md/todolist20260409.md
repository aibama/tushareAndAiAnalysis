你是python后端开发的专家，理解项目所有的代码，将项目应用层所有读取项目的配置文件的代码，改成stash进行获取，
项目的环境和依赖已经准备好：conda activate py_newest_stock
pip install stash-client
另外stash客户端需要加密，使用如下密码，参考代码如下：
# 启用客户端加密，服务端看到的只是密文
zk_client = Client("http://192.168.1.100:8080", zk_key="dzs940611")
zk_client.set("app/secrets/api-key", "secret-value")

section1:
你是python后端开发的专家，先理解项目所有的代码，昨天完成了将项目的配置文件转成从stash存取，现在进一步完善，将数据库、redis的连接配置也放入进去stash，从stash获取，数据库的ip和stash的ip一致，但是数据库的配置要用另外属性标记

section2:
bugfix:stash的配置读取错误，同时也检查其他组件，如数据库等类似的，确保不发生同类型错误。日志如下：
