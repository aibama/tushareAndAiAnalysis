你作为python后端开发专家，阅览和理解项目所有的相关代码和文档，
现在需要进行以下功能的开发：
1、连接pgsql数据库，主要的db是stock_data和表如下：
CREATE TABLE IF NOT EXISTS public.stock_minute_trades
(
    id integer NOT NULL DEFAULT nextval('stock_minute_trades_id_seq'::regclass),
    "time" timestamp without time zone NOT NULL,
    stock_id integer,
    price numeric(10,2),
    volume integer,
    direction character varying(10) COLLATE pg_catalog."default",
    pre_price numeric(10,2),
    stock_code character varying(12) COLLATE pg_catalog."default",
    CONSTRAINT stock_minute_trades_pkey PRIMARY KEY (id),
    CONSTRAINT stock_minute_trades_stock_id_time_key UNIQUE (stock_id, "time"),
    CONSTRAINT stock_minute_trades_stock_id_fkey FOREIGN KEY (stock_id)
        REFERENCES public.stock_basic (id) MATCH SIMPLE
        ON UPDATE NO ACTION
        ON DELETE NO ACTION
)
2、根据stock_minute_trades的time字段是当天的15：00：00，将这条记录的price和stock_code插入到
mysql的stocktradetodayinfo中，
CREATE TABLE `stocktradetodayinfo` (
  `id` decimal(16,2) DEFAULT NULL,
  `ts_code` varchar(255) NOT NULL,
  `amount` decimal(16,3) DEFAULT NULL,
  `echange` float DEFAULT NULL,
  `close` float DEFAULT NULL,
  `high` float DEFAULT NULL,
  `low` float DEFAULT NULL,
  `open` float DEFAULT NULL,
  `pct_chg` float DEFAULT NULL,
  `pre_close` float DEFAULT NULL,
  `trade_date` datetime(6) DEFAULT NULL,
  `vol` decimal(16,2) DEFAULT NULL,
  `trade_date_tmp` datetime(6) DEFAULT NULL,
  KEY `idx_stocktradedailyinfo_trade_date` (`trade_date`),
  KEY `idx_ts_code_date` (`ts_code`,`trade_date` DESC),
  KEY `idx_trade_date_ts_code` (`trade_date`,`ts_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb3;
high就是stock_minute_trades个股当日记录的最高的price，low雷士，open等于当天的09：25：00的price,vol等于当日记录的volume总和，amount等于当日记录的volume*price的，trade_date写入当天的日期。

pgsql配置信息：
spring.datasource.postgres.driverClassName=org.postgresql.Driver
spring.datasource.postgres.url=jdbc:postgresql://localhost:5432/stock_data
spring.datasource.postgres.username=postgres
spring.datasource.postgres.password=940611


