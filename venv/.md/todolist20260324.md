section1:
你是python后端开发的专家，对这个项目也比较熟悉，请你回顾和理解项目的代码和文档，尤其是PatternAnalysis\strategy\zscore和市值计算的服务（计算后会缓存到redis的stock_rank下的market_cap目录的服务），将zscore计算的，整合进来原有的市值服务（之前是市值数据暂时使用默认值（100亿），如需准确值需要接入tushare）

section1：
你是python后端开发的专家，对这个项目也比较熟悉，请你回顾和理解项目的代码和文档，在PatternAnalysis\emlevel2（目录已有已建）开发以下需求的接口，swagger可以调用：
1、个股的最近一个交易日的收盘价的+-30%的价位（每一分价格区间）的每分钟的成交量的历史情况统计接口，即通过mysql的stocktradetodayinfo的ts_code，close字段（个股的最近一个交易日的收盘价）、关联pygsql的stock_minute_trades的level2每分钟的每笔成交明细，所以这个接口需要计算每分钟的各个价位的，历史成交笔数、大单的成交量，大单的笔数（每笔多少volume可以配置文件定义），注意关联pygsql的stock_minute_trades的level2每分钟的每笔成交明细需要考虑性能，因为数据量很大，可以用到pgsql的时间窗口插件等技术